const matchMaker = require('./Users/user')

async function matchUsers() {
    const users = "GET request to fetch all users";

    const matchedPairs = [];

    for (let i = 0; i < users.length; i++) {
        for (let j = i + 1; j < users.length; j++) {
            
        }
    }
}