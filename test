// const { expect } = require('chai');
const chai = require('chai');
const chaiHttp = require('chai-http');
const { getUsers } = require('./backend/controllers/userController');

// describe('matchMaker', () => {
//     it('should match users based on criteria', async () => {
//         const user1 = {
//             Major: 'Biology',
//             subject: 'Chemistry',
//             skillLevel: 'Advanced',
//         };

//         const user2 = {
//             Major: 'Biology', // Same major as user1
//             subject: 'Physics',
//             skillLevel: 'Intermediate',
//         };
//         const result = await matchUsers([user1, user2]);

//         expect(result).to.deep.equal([user1, user2]);
//     });

// });


chai.use(chaiHttp);
const expect = chai.expect;

describe('User Controller', () => {
    it('should return an array of users', async function() {
        this.timeout(5000); // Increase the timeout to 10 seconds
        const result = await getUsers();
        expect(result).to.be.an('array');
        done();
    });

it('should return status 200 when fetching users', (done) => {
    chai
    .request('http://localhost:5000')
    .get('/api/users')
    .end((err, res) => {
        expect(res).to.have.status(200);
        done();
    });
});
});